// = assignment operator
// == comparison operator
// === strict equality operator

// to check same value as well as data type

let x = "3.14";

if (x === 3.14) {
    console.log("That is pi");
}
else {
    console.log("That is NOT pi");
}